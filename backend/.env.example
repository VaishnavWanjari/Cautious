# Copy to `.env` and adjust. All values are optional — the app runs fully
# offline without any of them.

# Enable the Claude-powered AI assistant (leave blank to stay offline).
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-4-8

# Seed the sample LNG Train 1 project on first launch (true/false).
SEED_ON_START=true
