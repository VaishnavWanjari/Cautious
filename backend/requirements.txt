# Minimum-version pins (>=) so pip can pick a wheel compatible with whatever
# Python you have (3.11-3.13). For a fully reproducible build, pin exact ==
# versions instead.

# ----- Web framework -----
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2

# ----- Persistence -----
SQLAlchemy>=2.0

# ----- Scheduling / network logic -----
# The CPM graph algorithms (topological sort, cycle detection) are implemented
# in pure Python under app/application/graph.py — no third-party graph library
# is required, keeping the engine dependency-free and fully offline.

# ----- Export -----
openpyxl>=3.1        # Excel .xlsx
reportlab>=4.1       # PDF generation

# ----- AI assistant (optional at runtime) -----
anthropic>=0.40

# ----- Tooling / tests -----
pytest>=8.0
httpx>=0.27
